language: python
python:
  - "3.5"
install:
  - "pip install -r requirements.txt"
  - "pip install coverage"
  - "pip install coveralls"
before_script:
  - "export PYTHONPATH=imeditor"
branches:
  only:
    - master
script:
  - "coverage run setup.py test"
after_success:
  - coveralls
